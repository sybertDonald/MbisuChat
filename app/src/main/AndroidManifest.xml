<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sy.infosys_ips">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Authentication.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Calender.CalenderActivity" />
        <activity android:name=".Calender.RecyclerPlanActivity" />
        <activity android:name=".Calender.PlanlayoutActivity" />
        <activity android:name=".Calender.ViewPlanActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Authentication.RegisterActivity" />
        <activity android:name=".Authentication.PasswordRecoverActivity" />
        <activity android:name=".Chat.ChatActivity" />
        <activity android:name=".Chat.MessageActivity" />
        <activity android:name=".Profile.EditProfileActivity"></activity>
    </application>

</manifest>